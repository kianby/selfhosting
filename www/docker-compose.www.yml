version: '3'

services:
  www:
    container_name: www
    image: kianby/www-madyanne
    restart: unless-stopped
    networks:
      - srv
    expose:
      - 80
    environment:
      - VIRTUAL_HOST=${DOMAIN},${HOST_WWW}.${DOMAIN}

